apiVersion: v1
kind: Service
metadata:
  name: {{ include "enderchest.fullname" . }}
  labels:
    {{- include "enderchest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enderchest.selectorLabels" . | nindent 4 }}
