meta:
  name: File Type Blocking - EXE
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/files/upload"
  
  headers:
    Authorization: "Bearer {{bearer_token}}"
    Content-Type: multipart/form-data

  body:
    multipartForm:
      - name: file
        type: file
        value: malware.exe

tests: |
  test("Upload EXE - Should Return 400", function() {
    expect(res.code).to.equal(400);
  });
  
  test("Response indicates invalid file type", function() {
    expect(res.body).to.include("InvalidFileTypeException");
  });
  
  test("Response contains error message", function() {
    expect(res.body).to.include("not allowed");
  });
